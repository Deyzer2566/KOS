#ifndef PROCESS_SETTINGS
#define PROCESS_SETTINGS
struct ProcessSettings{
	unsigned int tacts; // Условная единица времени, выделенного процессу.
											// 1 такт ~ период между тактами таймера
};
#endif